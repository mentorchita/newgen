name: WFCCI

on:
  workflow_dispatch:
jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: soichisumi/issue-copy-action@v0.2.0
      with:
        keyword: "/copy"
        targetRepository: mentorchita/GenerationTech
        githubToken: ${{ secrets.SECTRET_TOCKEN }}
        
